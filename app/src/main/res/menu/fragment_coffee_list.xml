<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <item
        android:title="@string/coffee_menu"
        android:icon="@drawable/ic_coffee_menu"
        android:id="@+id/coffee_menu"
        app:showAsAction="always"/>
    <item
        android:id="@+id/favorited_menu"
        android:icon="@drawable/ic_favorited"
        android:title="@string/favorited_menu"
        app:showAsAction="always"/>
    <item
        android:id="@+id/cart_menu"
        android:icon="@drawable/ic_shopping_cart"
        android:title="@string/shopping_cart"
        app:showAsAction="always"/>



</menu>